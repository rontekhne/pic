/* exe1202. Locate the system header files <stdio.h>, <limits.h>, and <float.h>
 * on your system (on Unix systems, look inside the /usr/include directory).
 * Examine the files to see what's in them.
 * by Ron, Feb 2021 */

